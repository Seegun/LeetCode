В файле указано описание задачи, собственное решение, далее закомментированное решение с сайта

Write a function createHelloWorld. It should return a new function that always returns "Hello World".
Напишите функцию создания Hello World. Он должен возвращать новую функцию, которая всегда возвращает "Hello World".
 
Кейсы когда должно работать:
Example 1:

Input: args = []
Output: "Hello World"
Explanation:
const f = createHelloWorld();
f(); // "Hello World"

The function returned by createHelloWorld should always return "Hello World".
Example 2:

Input: args = [{},null,42]
Output: "Hello World"
Explanation:
const f = createHelloWorld();
f({}, null, 42); // "Hello World"

Any arguments could be passed to the function but it should still always return "Hello World".
Функции могут быть переданы любые аргументы, но она все равно всегда должна возвращать "Hello World".

Код который был на сайте:

/**
 * @return {Function}
 */
var createHelloWorld = function() {
    return function(...args) {
       
    }
};

/**
 * const f = createHelloWorld();
 * f(); // "Hello World"
 */

Моё решение: 


/**
 * @return {Function}
 */
var createHelloWorld = function() {
    return function(...args) {
       return "Hello World"
    }
};

/**
 * const f = createHelloWorld();
 * f(); // "Hello World"
 */

Как решал:
Ну тут я даже не знаю что написать 
попросили сделать так чтобы функция возвращала всегда "Hello World" сделал ретерн в функции и вернул)
