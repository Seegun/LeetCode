// Задача: https://leetcode.com/problems/is-object-empty/?envType=study-plan-v2&envId=30-days-of-javascript
// Код на сайте: 

/**
 * @param {Object|Array} obj
 * @return {boolean}
 */
var isEmpty = function(obj) {
    
};

// Моё решение:

/**
 * @param {Object|Array} obj
 * @return {boolean}
 */
var isEmpty = function(obj) {
    if (Object.keys(obj).length == 0) {
        return true;
    } else {
        return false;
    }
    
};

// как решал:
// тут проверка если объект пустой то верну true если не пустой false
