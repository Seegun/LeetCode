// Задача: https://leetcode.com/problems/create-hello-world-function/?envType=study-plan-v2&envId=30-days-of-javascript
// Код который был на сайте:

/**
 * @return {Function}
 */
var createHelloWorld = function() {
    return function(...args) {
       
    }
};

/**
 * const f = createHelloWorld();
 * f(); // "Hello World"
 */

// Моё решение: 


/**
 * @return {Function}
 */
var createHelloWorld = function() {
    return function(...args) {
       return "Hello World"
    }
};

/**
 * const f = createHelloWorld();
 * f(); // "Hello World"
 */

// Как решал:
// Ну тут я даже не знаю что написать 
// попросили сделать так чтобы функция возвращала всегда "Hello World" сделал ретерн в функции и вернул)
